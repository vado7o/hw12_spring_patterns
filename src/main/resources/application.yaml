spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gb
    username: gb
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show_sql: true